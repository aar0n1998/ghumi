# Ghumi is a standalone project — always resolve from the public npm registry.
# Without this, a globally-configured private registry (e.g. a work CodeArtifact
# repo in ~/.npmrc) would be inherited here and fail once its token expires.
registry=https://registry.npmjs.org/
