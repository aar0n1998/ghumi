import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ExploreScreen() {
  return (
    <ScreenPlaceholder
      icon="map.fill"
      title="Explore"
      description="Discover destinations, saved places and trip ideas. Not built yet."
    />
  );
}
